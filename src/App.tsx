import React from 'react';
import classNames from 'classnames';
import './App.css';

const App = () => {
  return (
    <div className={classNames('root', 'gray-bg')}>
      test
    </div>
  );
}

export default App;
